export default class Add {

    constructor() {
        console.log('Addition module');
    }

    public add(firstNumber: number, secondNumber: number): number {
        return (firstNumber + secondNumber);
    }
}
