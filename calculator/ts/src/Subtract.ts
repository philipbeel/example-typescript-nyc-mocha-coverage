export default class Subtract {

    constructor() {
        console.log('Subtraction module');
    }

    public subtract(firstNumber: number, secondNumber: number): number {
        return (firstNumber - secondNumber);
    }
}
